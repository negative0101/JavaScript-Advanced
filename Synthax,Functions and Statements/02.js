function solution(a,b,c){
    let total_sum =a.length + b.length + c.length;
    let avrg = Math.floor(total_sum / 3)


    console.log(total_sum)
    console.log(avrg)
}
solution('chocolate', 'ice cream', 'cake')
solution('pasta', '5', '22.3')