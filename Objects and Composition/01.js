function solve(name, population, treasury) {

    const cityRecord = {
        name: name,
        population: Number(population),
        treasury: Number(treasury)
    }
    return cityRecord
}
solve('Tortuga',
    7000,
    15000)
solve('Santo Domingo',
    12000,
    23500)