function solve(arr) {
    let pairs = 0
    for (let row = 0; row < arr.length; row++) {
        for (let col = 0; col < arr[row]; col++) {
            let current = arr[row][col]
            let next = arr[row][col + 1]
        }
    }
}


solve([['2', '3', '4', '7', '0'],
['4', '0', '5', '3', '4'],
['2', '3', '5', '4', '2'],
['9', '8', '7', '5', '4']])